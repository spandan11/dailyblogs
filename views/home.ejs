<%- include("partials/header") -%>

    <h1>Home</h1>
    <p>
        <%= homeContent %>
    </p>

    <% blogs.forEach((blog)=> { %>
        <a href="/post/<%= blog.title %>" style="text-decoration: none" >
            <h1>
                <%= blog.title %>
            </h1>
        </a>

        <p>
            <%= blog.body.substring(0,100) + " ..."%>
            <a href="/post/<%= blog.title %>">Read More</a>
        </p>
        <%});%>

            <%- include("partials/footer") -%>